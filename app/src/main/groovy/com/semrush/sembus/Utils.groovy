package com.semrush.sembus

import java.text.SimpleDateFormat

class Utils {
    final static WIDGET_UPDATE_ACTION = "com.semrush.intent.action.UPDATE_WIDGET"
    final static TIME_FORMAT = new SimpleDateFormat("HH:mm")
}